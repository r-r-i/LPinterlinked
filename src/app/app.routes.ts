import { Routes } from '@angular/router';
import { PredatorCapComponent } from './shared/components/predator-cap/predator-cap.component';

export const routes: Routes = [
    {
        component: PredatorCapComponent,
        path: " "
    }
];
